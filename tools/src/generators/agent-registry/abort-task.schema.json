{
  "$schema": "https://json-schema.org/schema",
  "$id": "AbortTask",
  "title": "Cleanup worktree without merging",
  "type": "object",
  "properties": {
    "slug": {"type": "string", "$default": {"$source": "argv", "index": 0}, "x-prompt": "Task slug"}
  },
  "required": ["slug"]
}
