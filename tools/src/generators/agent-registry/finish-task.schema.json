{
  "$schema": "https://json-schema.org/schema",
  "$id": "FinishTask",
  "title": "Cleanup worktree and mark spec as done",
  "type": "object",
  "properties": {
    "slug": {"type": "string", "$default": {"$source": "argv", "index": 0}, "x-prompt": "Task slug"}
  },
  "required": ["slug"]
}
