{
  "$schema": "https://json-schema.org/schema",
  "$id": "StartTask",
  "title": "Create branch and worktree for a task",
  "type": "object",
  "properties": {
    "slug": {"type": "string", "$default": {"$source": "argv", "index": 0}, "x-prompt": "Task slug"}
  },
  "required": ["slug"]
}
